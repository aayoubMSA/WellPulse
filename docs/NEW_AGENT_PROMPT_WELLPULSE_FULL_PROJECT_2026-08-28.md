# New Agent Prompt — WellPulse Full Project Ownership

Take ownership of the **entire WellPulse research project** from the canonical private repository `aayoubMSA/WellPulse`, branch `main`.

Do **not** reconstruct current state from conversation memory.

First read completely, in this exact order:

1. `HANDOVER_CURRENT.md`
2. `docs/WELLPULSE_FULL_PROJECT_HANDOVER_2026-08-28.md`
3. `AGENTS.md`
4. then follow the complete mandatory read order specified in the full-project handover.

You are taking ownership of WP0-WP5 as one project, but you must resume execution only at the current gating frontier:

`WP2-P7B-H2 — CONTROLLER/RESTORE-DOMAIN CONTRACT AMENDMENT QA + FUTURE REQUALIFICATION AUTHORITY DECISION`

H2 is **OFFLINE / NOT STARTED**.

Do not contact POWDER, create a reservation, SSH to the testbed, alter RF, restart services, retry B1, execute W1/B2, rerun Golden, recalibrate H, teardown anything, begin WP3, or perform scored execution unless I later give separate explicit authorization.

Preserve the frozen aborted-attempt state exactly:

`B1=NULL_ABORTED_AFTER_Q3`

`W1=NOT_STARTED`

`B2=NOT_STARTED`

`AUTOMATIC_RETRY=NO`

`MANUAL_RETRY=NO_UNDER_CURRENT_CONTRACT`

`SCORED=NO`

The H1 closure is authoritative:

`WP2_P7B_H1=PASS_ABORT_EVIDENCE_FROZEN_ROOT_CAUSE_CLASSIFIED`

with first technical root cause:

`FIRST_TECHNICAL_ROOT_CAUSE=CONTROLLER_SESSION_COLLISION_SERVICE_RESTORE_KILLED_OPERATOR_TMUX_UE`

Do not reinterpret missing `restart_proof.json` as evidence that the gateway restart did not occur; the frozen H1 evidence establishes that the gateway restart occurred before the controller died.

The scientific controls remain frozen unless changed only through a separately reviewed prospective amendment. In particular preserve Q0/Q1/Q2/Q3=`0/40/52/55 dB`, attenuators `[1,33,2,34]`, pre-Q0=`60 s`, Q3=`120 s`, restart offset=`60 s`, `H_app=300 s` from `t_service_ready`, distinct `t_rf_restore/t_service_ready/t_app_complete`, primary cohort cutoff=`t_rf_restore`, and exact cell order `B1 -> W1 -> B2`.

Every future manual command block must explicitly name `nuc1 / CORE` or `nuc2 / UE`. Do not add diagnostic sleep/wait delays to human-operated scripts.

Work in finite patches. For H2, own only the bounded offline tasks defined in the canonical handover, run adversarial/static QA, issue a clear PASS/BLOCKED verdict, update canonical handover/provenance, and STOP.

Before editing any file, first report:

- retrieved canonical whole-project state;
- WP0-WP5 status and scientific weighted completion;
- current WP2/P7B frontier;
- exact aborted B1 scientific verdict;
- H1 classified root cause;
- frozen scientific controls;
- exact finite H2 patches you will own;
- explicit confirmation that live POWDER authority is currently `NO`.

Then STOP and wait for my authorization to continue.
