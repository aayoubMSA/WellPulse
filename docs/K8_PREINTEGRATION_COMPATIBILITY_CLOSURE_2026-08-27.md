# WP2 K8 — Pre-Integration Compatibility Closure — 2026-08-27

## Verdict

`K8=PASS`

`PRE_INTEGRATION_COMPATIBILITY_GATE=PASS`

This closure does **not** authorize Golden or scored science by itself.

`LIVE_HCI_AND_RAW_EVIDENCE_GATE=BLOCKED`

`REBOOK_GOLDEN=false`

`scored_runs_authorized=false`

## Evidence basis

### Successful bounded compatibility reservation

GitHub Actions run: `33085406598`  
Workflow: `.github/workflows/wp2-kfastlane-live-compat-v2.yml`  
Trigger commit: `a5ae5a7a3b88434d5036069533b3d54acc6ad476`  
Experiment UUID: `fc7c2187-2376-4a92-8de1-4665a06ea943`  
Classification: `INFRASTRUCTURE_ONLY_NON_SCORED`

No Golden workload, H calibration, or scored run was authorized.

### K2 — controller/off-POWDER evidence transport

PASS.

Observed:

- `/proj/WellPulse` bytes were pulled to the controller;
- controller bundle SHA-256: `f5464e08b41e2bcb81facd26daa2ee11ad115fa06554d40eea9bc01e0b0e6616`;
- GitHub Actions artifact ID: `9652138428`;
- uploaded artifact ZIP digest: `7d0333519e4446ed216672e86962d59973a947b0392680e6249028a604292cc2`;
- independent download/read-back completed;
- outer bundle hash and internal hashes verified;
- `CONTROLLER_OFFPOWDER_GATE=PASS`;
- `EVIDENCE_ESCROW_GATE=PASS`;
- `TEARDOWN_AUTHORIZED=YES`.

`K2=PASS`

### K3 — Portal lifecycle/status/identity contract

PASS.

The frozen Portal client revision remained:

`01be03b2f60c067815a7654437320dd981ca3617`

Live status sequence reached READY:

`provisioning -> provisioned -> ... -> ready`

The authoritative record guard verified:

- status `ready`;
- exact experiment ID match;
- unique expiry field `$.expires_at`;
- expiry `2026-08-27T16:00:53Z`.

Post-live offline K3 contract QA rerun `33087174307` concluded `success`.

`K3=PASS`

### K4 — detached receiver/process launch semantics

PASS.

The bounded detached SSH launch returned in `1 s`, below the `15 s` acceptance bound, and the remote PID/readiness checks passed.

`K4_LIVE_DETACH_GATE=PASS`

`K4=PASS`

### K5 — reservation time budget

PASS.

At the live gate:

- checked UTC: `2026-08-27T15:06:10Z`;
- expiry: `2026-08-27T16:00:53Z`;
- remaining: `3283 s`;
- minimum required: `2700 s`.

`PRELAUNCH_TIME_GATE=PASS`

`K5=PASS`

### K6 — persistent `/proj/WellPulse` and cross-node evidence path

PASS.

Both nodes resolved to the frozen profile topology:

- `enb1 -> nuc1`;
- `rue1 -> nuc2`;
- hardware `nuc5300`;
- image `urn:publicid:IDN+emulab.net+image+PowderProfiles:U18LL-SRSLTE:1`;
- profile revision `a6da96560b6526dc6816761282722c996418fd8c`.

Cross-node `/proj/WellPulse` write/read/hash verification passed before controller pull and independent artifact round-trip.

`K6_CROSS_NODE_PROJ_GATE=PASS`

`K6_LIVE_PERSISTENCE_AND_PULL=PASS`

`K6=PASS`

### K7 — safe observation semantics

PASS.

The compatibility run executed no RF observation command:

`K7_RF_OBSERVATION_COMMAND_EXECUTED=NO`

The dedicated semantic active-workflow guard was rerun after replacing the defective `! grep` assertion. Run `33087181821` concluded `success`.

The integrated static acceptance run `33087199247` also concluded `success` with the semantic K7 guard.

Frozen rule remains:

- `tmcc attenuator` is mutating/unsafe for observation;
- no independent unqualified RF probe during protected science;
- `HCI_CONTROL_ACTIONS_ENABLED=false`.

`K7=PASS`

## Runtime and supply-chain closure

The compatibility run reverified:

- runner `ubuntu-24.04`, exact observed image `20260823.283.1`;
- Portal client immutable revision;
- profile revision `a6da96560b6526dc6816761282722c996418fd8c`;
- system Python `3.6.9` on both nodes;
- OpenSSL `1.1.1 11 Sep 2018` on both nodes;
- base-image `mosquitto` executable absent on both nodes;
- `/proj/WellPulse` writable on both nodes.

The scientific runtime remains generated by the frozen bootstrap contract:

- uv `0.12.1` with frozen archive SHA-256;
- isolated Python `3.11.13`;
- `paho-mqtt=2.1.0`;
- rclone `1.75.0` with frozen archive SHA-256.

The live profile/image/revision was the same qualified path used by the prior bootstrap evidence. No moving supply-chain reference is accepted.

## Cleanup semantics

The successful compatibility run completed its mandatory termination call with:

`COMPAT_CLEANUP=TERMINATE_REQUESTED`

The same frozen terminate path had already been independently verified on the earlier failed compatibility experiment by subsequent read-only Portal queries returning `No such experiment` and absence from `experiment list`. Therefore termination behavior is accepted as bounded for K8; future scientific teardown remains additionally protected by the evidence escrow gate.

## Final K-series status

- `K1=PASS`
- `K2=PASS`
- `K3=PASS`
- `K4=PASS`
- `K5=PASS`
- `K6=PASS`
- `K7=PASS`
- `K8=PASS`

`PRE_INTEGRATION_COMPATIBILITY_GATE=PASS`

## What remains outside K8

The separate scientific-support prerequisite remains open:

`LIVE_HCI_AND_RAW_EVIDENCE_GATE=BLOCKED`

Golden remains unauthorized until that gate passes.

Next mission step:

`HCI/raw-evidence gate -> one clean non-scored Golden -> requalify/freeze H -> close WP2 -> WP3 conducted-RF confirmatory campaign`.
