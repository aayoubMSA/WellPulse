# WP2-P6 — Short Pre-Golden QA — 2026-08-27

## Verdict

`P6_SHORT_QA=PASS_AFTER_EXPERIENCE_LEDGER_HARDENING`

`USER_EXPLICIT_CONTINUATION=YES`

`LIVE_HCI_AND_RAW_EVIDENCE_GATE=PASS`

`PRE_INTEGRATION_COMPATIBILITY_GATE=PASS`

`P6_EXPERIENCE_LEDGER_INJECTION=PASS`

`scored_runs_authorized=false`

This QA is a bounded pre-execution recheck immediately before WP2-P6. It does not alter frozen science and does not itself create a POWDER reservation.

## Checks

1. Canonical P5 closure state was recovered before P6 activation; P1-P5 remain closed, P6 is the next bounded patch, scientific completion remains 20%, and scored execution remains prohibited.
2. Passive HCI implementation `scripts/wp2_golden_hci_emit.py` emits bounded local JSONL/stdout state only. It contains no SSH/API/tmcc/network-probe/control path and keeps `hci_control_actions_enabled=false` and `independent_probes=DISABLED`.
3. `.github/workflows/wp2-golden-offline-qa.yml` compiles the HCI emitter and retains reconstruction, persistent escrow, independent controller round-trip, and fail-closed teardown checks. It performs no POWDER contact or scored execution.
4. Exact Golden evidence inventory/finalization remains frozen. HCI is non-authoritative/conditional; scientific raw evidence remains mandatory and independent.
5. No background/in-run `/proj` checkpoint is introduced during protected G3-G7 science. G9 persistent escrow remains post-reconstruction.
6. Teardown authority remains impossible on the node/persistent side and requires independent controller artifact round-trip plus outer/internal SHA-256 verification.
7. Frozen science remains unchanged: Q0/Q1/Q2/Q3=`0/40/52/55 dB`, attenuation IDs `1 33 2 34` coupled, `H_app=300 s` from `t_service_ready`, H1 adverse evidence preserved, K1-K8 closed.
8. P6 has a dedicated bounded live workflow rather than reactivating historical K/A3 launchers.
9. The P6 live workflow performs its own pre-mutation offline QA in the same run before any reservation.
10. The P6 controller preserves the frozen profile, bindings, `nuc5300` hardware expectation, image, profile revision and 45-minute minimum post-READY time budget.

## Experience-ledger review

Canonical review record:

`docs/WP2_P6_EXPERIENCE_LEDGER_INJECTION_2026-08-27.md`

The Drive `Physical Validation Asset Ledger` and `Research Operating Doctrine — Evidence-First Execution v2.1` added four explicit safeguards:

1. node-local home is transient evidence storage; `/proj` plus verified off-platform read-back is mandatory before teardown;
2. resource catalog entries prove capability, not current availability; availability is checked at reservation time and cannot justify silent hardware/profile substitution;
3. experiment/run identity and failure evidence must survive fail-closed automation paths;
4. remote POWDER evidence supports wireless/network/edge claims only and cannot be relabeled as field/agronomic validation.

A concrete QA defect was found before trigger: the original P6 workflow could lose structured experiment UUID/run outputs when the execute step returned non-zero. This is unacceptable if protected science had started and the experiment had to remain live. The workflow was hardened before launch so `/tmp/wp2-p6/result.env` is exported to workflow outputs even on failure, preserving whatever authoritative identity/evidence pointers had been reached.

The same pre-mutation workflow now statically asserts:

- exact `nuc5300` hardware contract;
- writable `/proj/WellPulse` gate exists before science;
- protected-science failure path prohibits automatic termination;
- no Drive/rclone evidence authority exists in the P6 execution controller.

## Resource preflight interpretation

The public POWDER status/resource page was also checked immediately before launch preparation. Its text-accessible representation did not expose usable dynamic per-resource free/busy values, so no availability claim is inferred from that view.

P6 therefore uses the frozen protocol semantics:

`RESOURCE_AVAILABILITY_PREFLIGHT=PASS|DEFER|UNKNOWN`

`UNKNOWN` is acceptable only as an advisory state; the authoritative decision remains Portal create/READY plus exact manifest identity and no substitution of the frozen design.

## P6 execution boundary

The user's explicit continuation authorizes proceeding from this QA into WP2-P6.

If the advisory preflight and authoritative Portal lifecycle/identity/time gates permit, execute exactly one non-scored Golden G0-G10. Require complete raw evidence, persistent `/proj` escrow, independent controller artifact round-trip, `EVIDENCE_ESCROW_GATE=PASS`, and `TEARDOWN_AUTHORIZED=YES` before teardown.

Failure before protected science may clean up the bounded reservation. Failure after protected science begins without evidence closure must leave the experiment live for evidence recovery.

P6 must STOP before WP2-P7 formal scored authorization.
