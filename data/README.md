# Data Policy

`data/raw/` is for immutable raw evidence when a remote run is exported locally. Large evidence should be stored in the controlled Drive raw-evidence folder and referenced by manifest rather than casually committed to Git.

Never replace a missing measurement with an estimate or regenerated value.
