[project]
name = "wellpulse"
version = "0.1.0"
description = "Offline-first resilience and remote-testbed validation harness for WellPulse"
requires-python = ">=3.10"

[project.optional-dependencies]
mqtt = ["paho-mqtt>=2.1,<3"]

[tool.setuptools]
package-dir = {"" = "src"}
