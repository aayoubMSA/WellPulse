# POWDER G3 simulated-stack validation — 2026-08-24

Status: **PROCESS PASS / PORTAL TEARDOWN PENDING**

Evidence class: **INFRASTRUCTURE_ONLY_NON_SCORED**

## Target identity

- Project: `WellPulse`
- Experiment: `WP-G3-SIMSTACK`
- Experiment UUID: `3484b01d-7eca-48e7-9e34-866680057b0d`
- Profile: `srsLTE-SIM:9`
- Profile UUID: `80dda605-7e5f-11e9-8006-e4434b2381fc`
- Hardware: one `d430`
- Allocated node observed in live portal: `pc757`
- SSH endpoint used: `pc757.emulab.net:22`
- Canonical remote hostname: `node.wp-g3-simstack.wellpulse.emulab.net`
- Image observed in live portal: `PowderProfiles/gnuradio-srslte`
- Remote OS/kernel: Ubuntu 18.04.1 LTS / Linux 4.15.0-33-generic x86_64

## Manual simulated-stack procedure

The current G3 experiment was provisioned manually from the current POWDER `srsLTE-SIM:9` profile. SSH used the previously validated `WellPulse-POWDER-Golden` identity. No GitHub CI SSH secret was used for the accepted process evidence.

Transmitter command:

```bash
/usr/local/srsLTE/build/lib/examples/pdsch_enodeb -o /tmp/wellpulse_g3.iq -n 5 -m 9 -v
```

Observed outcome:

- transmitter initialized the 25-PRB LTE downlink stack;
- repeated PDSCH encoding occurred at MCS 9 / QPSK;
- command completed normally with terminal message `Done` and returned to the shell;
- a separate transmitter exit-code capture was not taken, so no numeric TX exit code is claimed here.

Receiver command:

```bash
/usr/local/srsLTE/build/lib/examples/pdsch_ue -i /tmp/wellpulse_g3.iq -n 5 -r 1234 -v -d
```

Observed outcome:

- MIB decoded successfully;
- CFI and PDCCH were decoded;
- PDSCH was detected for RNTI `0x1234`;
- code-block CRCs reported `CRC=OK`;
- multiple transport blocks reported `TB decoded OK`;
- receiver terminated normally with `Bye`;
- captured immediately afterward: `RX_RC=0`.

One diagnostic line on the first decoded transport block stated `Error in TB parity: par_tx=0x0, par_rx=0x0`; subsequent transport blocks decoded successfully and the receiver exited with code 0. This line is preserved rather than suppressed and is not treated as a fatal process failure.

## Generated waveform evidence

Before deletion:

- `IQ_BYTES=2304000`
- SHA-256: `103de59d52e75252e916d7ed62c5c9b76401e817ffec3178363879e0bed71678`

Observed UTC metadata:

- remote UTC: `Mon Aug 24 20:51:44 UTC 2026`
- hostname: `node.wp-g3-simstack.wellpulse.emulab.net`

The temporary waveform was then deleted manually:

```bash
rm -f /tmp/wellpulse_g3.iq
test ! -e /tmp/wellpulse_g3.iq && echo "IQ_CLEANUP=PASS"
```

Observed: `IQ_CLEANUP=PASS`.

SSH session then exited cleanly and the connection to `pc757.emulab.net` closed.

## Gate interpretation

**G3 simulated stack/data path: PROCESS PASS.**

This evidence demonstrates only the profile-authoritative file-based simulated path:

`pdsch_enodeb -> IQ file -> pdsch_ue`

It does **not** demonstrate SDR operation, RF propagation, controlled physical-RF impairment, MQTT/WellPulse scientific workload performance, or any scored WP-PWD01 result.

Overall G3 remains **INCOMPLETE** until POWDER portal teardown is explicitly verified as destroyed/terminated/absent and current resource usage returns to zero.

Scientific completion remains **20%** and `scored_runs_authorized=false`.
