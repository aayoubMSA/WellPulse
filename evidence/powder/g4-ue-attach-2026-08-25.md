# POWDER G4 — UE Attach + LTE User-Plane Checkpoint — 2026-08-25

**Evidence class:** NON-SCORED INFRASTRUCTURE QUALIFICATION  
**Experiment:** `WellPulse/WP-G4-CTRL-RF`  
**Experiment UUID:** `0e4269fb-06dd-432b-abec-4bca685a05af`  
**Gate:** G4 controlled physical-RF lifecycle — IN PROGRESS pending teardown verification  
**Scored scientific runs:** 0  
**`scored_runs_authorized`:** false

## Live role binding for this rerun

The 2026-08-25 late-session rerun bound roles opposite to the earlier session:

- `enb1` -> physical `nuc2` (network/base-station side)
- `rue1` -> physical `nuc1` (UE/device side)

The user fixed the local screen layout as LEFT=`nuc1` and RIGHT=`nuc2` for operational clarity.

## Startup state

On `nuc2` (network/base-station side), `/local/repository/bin/start.sh` launched the EPC/eNodeB path. A later read-only process check confirmed:

```text
1374 sudo srsepc /etc/srslte/epc.conf
1375 srsepc /etc/srslte/epc.conf
1431 sudo srsenb /etc/srslte/enb.conf
1432 srsenb /etc/srslte/enb.conf
```

On `nuc1` (UE/device side), `/local/repository/bin/start.sh` launched the srsUE path on the physical B210.

## UE attach evidence

The live EPC/eNodeB pane on `nuc2` showed the following credential-free control-plane sequence:

```text
SPGW: Allocated UE IP 172.16.0.2
Adding attach accept to Initial Context Setup Request
Sent Initial Context Setup Request. E-RAB id 5
Received Initial Context Setup Response
E-RAB Context -- E-RAB id 5
UL NAS: Received Attach Complete
Received GTP-C PDU. Message type: GTPC_MSG_TYPE_MODIFY_BEARER_REQUEST
```

The UE pane on `nuc1` explicitly reported:

```text
RRC Connected
Network attach successful. IP: 172.16.0.2
```

The eNodeB pane reported:

```text
==== eNodeB started ====
User 0x46 connected
```

A later RRC release moved the UE to idle. This did not invalidate the EPS session. When user traffic was generated, the EPC/eNodeB logged a new service request and the UE re-established radio connectivity as `User 0x47 connected`.

## LTE tunnel/interface evidence

On `nuc1`:

```text
tun_srsue  UNKNOWN  172.16.0.2/24
```

On `nuc2`:

```text
srs_spgw_sgi  UNKNOWN  172.16.0.1/24
```

These addresses identify the UE LTE tunnel and EPC SGi-side endpoint used for the bounded user-plane test.

## Bounded LTE user-plane packet test

Command executed on the UE side (`nuc1`):

```bash
ping -I tun_srsue -c 5 172.16.0.1
```

Observed result:

```text
64 bytes from 172.16.0.1: icmp_seq=1 ttl=64 time=521 ms
64 bytes from 172.16.0.1: icmp_seq=2 ttl=64 time=21.4 ms
64 bytes from 172.16.0.1: icmp_seq=3 ttl=64 time=20.0 ms
64 bytes from 172.16.0.1: icmp_seq=4 ttl=64 time=18.7 ms
64 bytes from 172.16.0.1: icmp_seq=5 ttl=64 time=36.8 ms

5 packets transmitted, 5 received, 0% packet loss, time 4004ms
rtt min/avg/max/mdev = 18.744/123.691/521.234/198.879 ms
```

Interpretation: **actual IP packets traversed the LTE UE tunnel successfully with 5/5 replies and 0% loss**. The first high-latency packet coincided with the UE waking from RRC idle and triggering a service request; this latency is retained as observed infrastructure behavior and is not treated as a scored performance result.

## Raw screenshot preservation

Canonical Drive raw-evidence workspace (`02_RAW_EVIDENCE`, id `11xaitxG0vkV6fCzK_JgLAacrdhjz7GBf`) contains:

- `POWDER_G4_UE_ATTACH_PASS_2026-08-25.png` — id `1wz4yl65L1moANwQRuJsgTJqQRCMq66pL`;
- `POWDER_G4_UE_ATTACH_AND_RRC_RELEASE_2026-08-25.png` — id `1AtGXXC2XV8KEEyJORuFQR6ZLUPi0KRk1`;
- `POWDER_G4_LTE_USER_PLANE_PING_PASS_2026-08-25.png` — id `1zCecU_szN7G-yITFdRHzj5EXR-xXUATE`.

## Verdict

The following G4 sub-gates are now PASS:

- physical B210-backed UE startup;
- successful radio access / RACH interaction;
- LTE attach completion;
- EPC session creation;
- UE IP allocation `172.16.0.2`;
- E-RAB/bearer context establishment;
- UE LTE tunnel present;
- bounded LTE user-plane packet transfer: **5/5 replies, 0% loss**.

Therefore the **LTE attach + bearer + user-plane qualification = PASS**.

## Evidence boundary

This is still **non-scored infrastructure qualification**. It does not establish RF impairment calibration, MQTT/WellPulse resilience effects, or any scored scientific observation. G4 should be classified fully PASS only after manual experiment teardown and verification of zero active usage / resource release.

## Exact next action

Terminate the POWDER experiment manually, verify it leaves the active-experiment list and that active usage returns to zero, then record G4 = PASS. Do not begin scored runs. The next scientific/infrastructure stage after closure is G5 / WP2 RF impairment and measurement calibration, subject to the separate comparator gate before any scoring.